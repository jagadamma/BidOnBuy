name: Node.js CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      # Save the original HOME value
ORIGINAL_HOME=$HOME

# Set the temporary HOME value
export HOME='/home/ec2-user/actions-runner/aap/BidonBuy-Client/actions-runner/_work/_temp/fa065aa6-a777-41e7-83c9-78f46971fc96'

# Make the desired Git config changes
git config --global <config_key> <config_value>

# Restore the original HOME value
export HOME=$ORIGINAL_HOME


jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 16
        
    - name: Install dependencies
      run: |
       sudo yum -y install curl
       curl -sL https://rpm.nodesource.com/setup_16.x | sudo bash -
       yum -y install nodejs npm
      
    - name: Run tests
      run: |
       yum install nginx -y
       yum -y install nodejs npm
       npm install --force
       npm i react-password-strength-bar --force

       npm run build
       cp -rf build/* /usr/share/nginx/html/
       sudo systemctl start nginx
       sudo systemctl restart nginx
